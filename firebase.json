{
  "functions": {
    "predeploy": [
      // "npm --prefix \"$RESOURCE_DIR\" run lint"
    ]
  },
  "firestore": {},
  "hosting" : {
    "public": "./view/build",
    "rewrites": [
      {
        "source": "/api/**",
        "function": "api"
      }
    ]
  }
}
