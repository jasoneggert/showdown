import React, { useState } from 'react';
import { useAuthedAxios } from '../../hooks/useAuthedAxios';
import axios from 'axios';
const DeathMatches = () => {
    return <div>DeathMatches</div>
}

export default DeathMatches;